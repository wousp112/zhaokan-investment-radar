# 测试与评测说明

## 2026年9月22日：影音交付与页面收尾

本轮122项Python测试、16项前端逻辑测试通过。新增9项检查覆盖缺失音轨、错误画幅、超出提交时长、章节断档及字幕重叠。实际MP4为146.23秒、1920×1080、30帧，带AAC旁白；完整解码与声音测量记录见[影音验证](artifacts/cinematic-v1.2/verification.json)。[后端原始结果](artifacts/cinematic-v1.2/tests.xml)和[前端原始输出](artifacts/cinematic-v1.2/frontend-tests.txt)独立保留。

本轮在独立模拟实例走过同一任务的创建、触发、去重、公告暂存和补发，再验证异常、恢复、反馈与修改后旧通知的依据。视频显示的1条及2条条件提醒来自实际界面读回，未修改源图中的判断数据。页面切换增加滚动复位；播放器增加章节定位与跳转后返回视频区域。浏览器验收另外记录。

以下9月21日的113项测试和无声视频说明属于历史版本。模型编译器本轮未改动，不新增开放域准确率或真实用户体验结论。

初始报告生成时间：2026-09-21T08:54:14.381790+00:00。下文保留各批次的结果；不同批次的数字不合并为一个准确率。

## 专业交付补齐后的验证

应用源码提交`8ea0820`对应的[GitHub Actions运行](https://github.com/wousp112/zhaokan-investment-radar/actions/runs/35613941075)已完成，结果为success。v1.1.0发布中的视频附件状态为uploaded，大小1,390,613字节，SHA256为`db035c8c50ff5c1e57d7bf545b87dc8397d82df72f905c8e3db06ea37a483f23`，与本地视频一致。公开入口后续经历一次通道故障和恢复，详见[部署记录](docs/DEPLOYMENT.md)。

2026年9月21日晚，后端113项回归全部通过，前端逻辑16项通过。新增覆盖创建重试、归档释放额度、已读与反馈、冻结通知依据、分页、来源能力限制、调度状态与访客隔离。原始结果见[后端JUnit](artifacts/professional-review/readiness-tests.xml)和[前端输出](artifacts/professional-review/frontend-tests.txt)。

当前编译器重新完成20条固定模型用例，20次实际取得DeepSeek输出且字段匹配，见[联网复测](artifacts/ai-evaluation-readiness-network-20260921.json)。编译器SHA256为 `18807046aef268a63c70e89c59c110e9659209885469f8ae798831d66e39302a`。此前受限网络无法解析域名，20次连接失败、20次本地回退匹配，见[失败批次](artifacts/ai-evaluation-readiness-20260921.json)。该失败批次没有计为模型成功。

公网浏览器另一次自然语言操作得到DeepSeek草稿，页面实际记录6001毫秒。直接选择条件的完整操作也已执行：保存新模拟任务，查看未触发依据，模拟下跌并检查去重，保留并补发新公告，再验证来源异常和恢复。打开通知使未读数减少；反馈能保存和撤回；原任务阈值改为4%后，旧通知仍保留3%的条件和触发时96.2元的依据。

容量探测使用真实调度器和隔离数据库，120条任务完成首轮检查，120条通知与120条判断记录一一对应，最高四路同时取数。首轮检查耗时1.446秒，来源是合成延迟接口，见[容量记录](artifacts/professional-review/capacity.json)。它不证明真实供应商吞吐或生产可用性。在线备份在隔离目录恢复，完整性及表行数核对通过，见[恢复记录](artifacts/professional-review/backup-restore.json)。

390像素手机视口中，通知窗口可视宽度与内容宽度均为341像素。原生图像返回工具未提供可见图像，截图已保存，但未完成逐像素视觉验收或完整辅助技术测试。本轮约96秒视频采用实际操作截图分镜，文件属性与完整解码验证见[视频记录](artifacts/professional-review/demo-video.json)。不同画面来自各自的真实操作步骤，不能以分镜时长推导执行耗时。

旧测试、旧模型批次和旧视频均保留。下面的87项、10项及11项操作验收属于此前版本。旧“最终模型复测”的源码摘要也只对应当时编译器，当前版本使用本节所列的联网复测记录。真实用户、留存、完整公告参考流和交易日持续观测尚未完成。

发布前复现CI的 `RADAR_ALLOW_LIVE=0` 配置时，3条量比用例先被全局“只开放模拟”限制拦住。测试现已明确开启其所测的真实数据能力，再核对未接入量比的拒绝行为，测试没有调用真实供应商。初次失败保存在 `artifacts/professional-review/ci-environment-tests.xml`，修复后结果单独保存在 `ci-environment-fixed.xml`，未改动产品的真实数据限制。

## 界面改版后的补充验证

2026年9月21日再次运行后端测试，87项通过。新增的显示层测试10项通过，并在美国西部时区复跑通过。原生Chrome完成新版创建、条件编辑、去重、公告保留与恢复等操作，390像素视口的页面宽度和规则窗口均没有横向溢出。

新公网入口已使用真实DeepSeek解析黄金示例，页面报告本次解析6126毫秒，随后成功创建模拟提醒并用模拟跌幅触发。这是一次新入口检查，不替代后文20条模型评测。

范围、截图和本轮原始测试输出见[改版验收记录](docs/REDESIGN_ACCEPTANCE.md)及[验收索引](artifacts/ux_redesign/native-acceptance.json)。原有浏览器验收、模型评测和视频记录保留为改版前证据。更新后的独立浏览器脚本和录制脚本通过语法检查，本轮没有重新执行这两个脚本；实际交互通过原生浏览器工具完成。

## 本轮证据

| 验证层 | 实际结果 | 证据 |
| --- | --- | --- |
| 单元与 API 自动化测试 | 87/87 通过，0 失败，0 跳过 | [JUnit 原始记录](artifacts/pytest-results.xml) |
| 本地 Chrome 操作验收 | 11 项通过，0 个未捕获脚本异常 | [原始记录](artifacts/browser-acceptance.json) |
| 独立进程强制终止与恢复 | 通过 | [进程级验证](artifacts/process-recovery.json) |
| 首轮真实模型调用与字段校验 | 15/20 次取得有效模型结果；其中 15 次字段匹配 | [20 条逐项记录](artifacts/ai-evaluation-baseline.json) |
| 含本地回退的规则编译 | 20/20 条输出匹配固定用例预期 | 同上；模型和回退分开计数 |
| 真实黄金用例模型调用 | deepseek，8453 ms | [首次真实探测](artifacts/live-probe.json) |
| 最新扶摇行情探测 | ok，市场开市状态为 False | [时间与请求号](artifacts/latest-quote-probe.json) |
| 公网 Chrome 操作验收 | 11 项通过 | [原始记录](artifacts/browser-public-acceptance.json) |

最终模型复测对应当前编译器源码：是。最新批次结果为 20/20，原始记录见 [最终模型复测](artifacts/ai-evaluation-final.json)。本轮还验证了真实报价恰好涨跌3%时，严格比较不会因浮点误差提前触发，见 `test_live_quote_keeps_exact_percentage_boundary`。

完整题目原句在解析边界修正后，又进行了真实浏览器复测：HTTP 200，确认窗口可见状态为 True。记录见 [修复后完整原句验证](artifacts/ui-parse-probe.json)；修复前返回422的原始记录保存在 [首次错误](artifacts/ui-parse-initial-error.json)。这次模型调用与上面的首轮20条评测分别记录。

这些是固定用例和本轮环境下的结果。没有进行大规模用户测试、随机开放域基准、全天交易日压力测试或生产可用性测量，不能用它们推导整体准确率或长期服务保证。

## 功能、故障与边界覆盖

| 要求 | 测试位置 | 验证对象 |
| --- | --- | --- |
| 自然语言与可修改规则 | `test_main_pipeline.py`、`test_api.py` | 20 条本地表达、标的上下文、严格和包含边界、持续时间、AND/OR、规则修改与版本冲突 |
| 生命周期与免打扰 | `test_cooldown.py` | 激活、暂停、恢复、到期、同日价格去重、公告终身指纹、冷却中新公告保留 |
| 每轮可解释性 | `test_cooldown.py` | 连续 50 次检查对应 50 条记录；逐条件结果、来源、时间、规则校验摘要 |
| 来源异常 | `test_degradation.py` | 超时、500、过期、冲突、公告失败、休市、健康提醒去重、恢复通知 |
| 恢复与一致性 | `test_cooldown.py`、独立进程脚本 | SQLite 回滚、并发检查只产生一条提醒、SIGKILL后恢复冷却及待提醒公告 |
| 用户与输入边界 | `test_api.py`、`test_regressions.py` | 会话隔离、跨站修改拒绝、大小限制、未知字段、真实任务拒绝模拟注入、交易与收益承诺拒绝 |
| 实际操作 | `scripts/browser_acceptance.py` | 浏览器创建、确认、触发、冷却、公告保留、故障恢复、版本、消息、390像素移动布局 |

自动化测试中的接口故障由明确的演示适配器或测试替身注入。真实网络探测单独记录，不能把故障注入结果解释成真实接口长期稳定运行。

## 真实模型：成功、失败和回退分别记录

首轮模型请求完成 20 条。15 次取得 DeepSeek 输出，5 次因连接错误回退。有效模型调用耗时中位数为 2344 ms，最短 1908 ms，最长 8104 ms。样本来自手工用例，未测用户自由输入分布。

| 编号 | 输入 | 实际引擎 | 规则字段 | 调用失败原因 |
| --- | --- | --- | --- | --- |
| AI01 | 贵州茅台日内跌幅达到3% | 模型输出 | 匹配 | 无 |
| AI02 | 未来两周监控贵州茅台，下跌3%提醒 | 模型输出 | 匹配 | 无 |
| AI03 | 未来7天监控宁德时代，跌幅达到5% | 模型输出 | 匹配 | 无 |
| AI04 | 未来一周监控平安银行，下跌达到2% | 模型输出 | 匹配 | 无 |
| AI05 | 未来三天监控招商银行，跌幅超过4% | 模型输出 | 匹配 | 无 |
| AI06 | 比亚迪上涨达到5%提醒我 | 模型输出 | 匹配 | 无 |
| AI07 | 中国平安涨幅超过2%提醒 | 模型输出 | 匹配 | 无 |
| AI08 | 同花顺涨3%提醒 | 模型输出 | 匹配 | 无 |
| AI09 | 五粮液跌幅达到1.5% | 模型输出 | 匹配 | 无 |
| AI10 | 长江电力上涨超过0.5% | 模型输出 | 匹配 | 无 |
| AI11 | 贵州茅台股价低于1500元提醒 | 模型输出 | 匹配 | 无 |
| AI12 | 宁德时代股价高于300元提醒 | 模型输出 | 匹配 | 无 |
| AI13 | 美的集团价格不高于50元提醒 | 模型输出 | 匹配 | 无 |
| AI14 | 工业富联股价不低于30元提醒 | 模型输出 | 匹配 | 无 |
| AI15 | 中芯国际量比达到3时提醒 | 模型输出 | 匹配 | 无 |
| AI16 | 未来两周贵州茅台跌3%或者发布新的业绩预告 | 本地回退 | 匹配 | ConnectError |
| AI17 | 贵州茅台跌3%并且出现业绩预告 | 本地回退 | 匹配 | ConnectError |
| AI18 | 贵州茅台发布新的业绩预告提醒 | 本地回退 | 匹配 | ConnectError |
| AI19 | 宁德时代发布新的业绩快报提醒 | 本地回退 | 匹配 | ConnectError |
| AI20 | 未来十天比亚迪发布新公告就提醒 | 本地回退 | 匹配 | ConnectError |

原始失败没有删除，也没有把回退记为模型成功。当前结果适合检查主链路与故障回退，不能声称“AI准确率100%”。

提示词修正后的第二批完整20条复测：20/20次取得模型输出且字段匹配；其中模型实际返回20次。全链路字段匹配20条。该批次独立保存，附当前编译器源码SHA256，见[最终模型复测](artifacts/ai-evaluation-final.json)。

## 进程恢复中实际发现并修复的问题

第一次强制终止测试在准备阶段失败：模拟公告时间被截断到秒，任务开始时间保留亚秒精度，导致同一秒内新增公告被误认为早于任务开始。

已保留公告完整时间精度，并新增 `test_new_event_keeps_subsecond_timestamp`。重跑后验证了任务冷却、待提醒公告和去重指纹在进程重启后保留；推进冷却后，只补发待提醒公告，没有重复价格提醒。测试仅终止脚本创建的独立进程，不影响演示服务。

## 复现

```bash
python -m pytest -q --junitxml=artifacts/pytest-results.xml
python scripts/test_process_recovery.py
python scripts/browser_acceptance.py
python scripts/evaluate_ai.py
python scripts/probe_quote.py
python scripts/build_verification_report.py
```

浏览器验收要求应用已启动，以及 Playwright 与 Chrome 可用。模型和行情脚本需要合法的服务凭据及网络，可能产生调用用量。每次重跑会写入该脚本对应的结果文件；需要比较不同批次时应先归档现有结果。
