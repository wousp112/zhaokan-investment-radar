# 照看 v1.0.0

可交互的投资监控与风险雷达，包含自然语言生成规则、人工确认与修改、持续检查、异常降级和逐条件解释。

价格与事件去重、冷却中新公告保留、规则版本与恢复记录均持久化保存。用独立服务进程强制终止后重启，验证了冷却和待提醒公告保留，以及提醒不重复。

交付前87项单元/API测试、11项公网Chrome交互检查通过。20条固定模型用例最终复测全部取得模型输出并匹配预期字段；历史失败与回退批次单独保留。

GitHub的Ubuntu运行器也已通过自动化测试，并完成Docker镜像构建、容器启动和HTTP主链路验收。对应运行记录与容器结果随源码提供。

附件包含96秒H.264视频、视频校验信息和完整源码压缩包。视频是实际Chrome操作录屏，带中文字幕，行情使用模拟模式。公网体验当前依赖本机和临时通道，长期部署边界见项目README。
