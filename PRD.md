# 照看 · 产品需求文档

版本：v1.2.1 提交核对版，2026-09-22。本文描述当前交付功能；后续计划单独列出。

## 用户与要解决的问题

面向有自选股和明确研究关注点、无法全天盯盘的 A 股个人投资者。用户需要把关注条件交给系统持续检查，并随时知道条件有没有理解对、检查是否正常、为什么提醒或没有提醒。此定位来自题目与产品设计假设，尚未通过真人访谈验证。

核心场景：

> 未来两周帮我盯住贵州茅台：日内跌幅达到3%，或者发布新的业绩预告，就提醒我。同一件事别反复提醒；如果监控出了问题，也告诉我。

## 用户流程与验收要求

| 步骤 | 用户操作与产品反馈 | 验收要求 |
| --- | --- | --- |
| 首次体验 | 教程引导用户用模拟示例创建提醒，也可直接选择条件 | 模拟数据标记清楚；确认前不启动任务 |
| 写下关注点 | 输入公司与条件，AI整理成规则；失败时显示本地解析结果 | 无法支持或存在歧义时要求补充，不静默删掉条件 |
| 核对规则 | 查看公司、比较关系、阈值与有效期，允许修改 | 跌幅达到3%为相对昨收变化≤-3%；业绩预告不扩展成业绩快报 |
| 开启监控 | 用户确认后保存并开始定期检查 | 展示状态、最近检查时间与检查依据 |
| 收到提醒 | 在站内查看触发条件与当时的证据，可标记已读和反馈 | 后续修改规则不能覆盖旧提醒的证据 |
| 管理任务 | 暂停、恢复、修改、到期和归档 | 修改生成新版本；历史版本恢复为新版本 |
| 检查异常 | 显示受影响的数据与条件，恢复后记录恢复通知 | 无效数据不能被当成“未达到条件”，真实数据失败不能切换成模拟行情 |

提醒仅保存在站内。关闭网页后后台仍可检查，但当前不发送手机推送、短信或邮件。服务停止期间不执行检查。

## 功能范围与优先级

本次必须完成自然语言到确认规则、持续检查、去重冷却、异常恢复和可追溯提醒。直接选条件、首次教程、提醒反馈与运行统计已实现，用于降低首次使用门槛和检查运行结果。

支持已核对名称和代码的12只A股；每个任务最多6条平铺条件，使用单一AND或OR组合。条件类型包括价格、相对昨收涨跌幅、公告、指定时间和量比。量比只在模拟模式可用；复杂嵌套与时序组合不在本次范围内。

## AI与程序分别做什么

DeepSeek（接口标识deepseek-flash）只负责把用户文字整理成结构化条件。程序核对公司代码、校验字段并生成任务；用户确认后才启用。模型不可用时尝试本地规则解析，并明确显示实际处理方式。

后续取数、阈值计算、去重和通知保存由程序执行。当前没有AI公告摘要、事件归因或自动交易功能。这样可以逐项检查判断依据，不必每轮调用模型。

## 统一任务模型

运行结构由[任务模型](app/models/dsl.py)定义，完整字段可从服务的`/api/schema`读取。下列示例使用模拟数据；任务状态独立保存，不混入规则定义。

```json
{
  "schema_version": "1.0",
  "version": 1,
  "user_intent_raw": "未来两周帮我盯住贵州茅台：日内跌幅达到3%，或者发布新的业绩预告，就提醒我。",
  "target": {
    "symbol": "600519.SH",
    "name": "贵州茅台",
    "market": "A_SHARE"
  },
  "validity": {
    "start_time": "2026-09-21T07:46:28.349406Z",
    "end_time": "2026-10-05T07:46:28.349406Z",
    "timezone": "Asia/Shanghai"
  },
  "condition_logic": "OR",
  "conditions": [
    {
      "id": "price_drop_3pct",
      "type": "PRICE_CHANGE_RATIO",
      "operator": "<=",
      "threshold": -0.03,
      "event_category": null,
      "at": null,
      "display_text": "相对昨收涨跌幅 <= -3%",
      "source": "fuyao_snapshot"
    },
    {
      "id": "new_performance_forecast",
      "type": "ANNOUNCEMENT_EVENT",
      "operator": "<=",
      "threshold": null,
      "event_category": "PERFORMANCE_FORECAST",
      "at": null,
      "display_text": "出现新的业绩预告",
      "source": "ifind_search"
    }
  ],
  "governance": {
    "frequency_seconds": 60,
    "trading_hours_only": true,
    "cooldown_minutes": 30,
    "deduplication_keys": [
      "announcement_id",
      "trading_date"
    ],
    "alert_channels": [
      "WEB_INBOX"
    ]
  },
  "data_mode": "replay"
}
```

## 长期运行规则

默认每60秒检查一次，冷却30分钟，均可在支持范围内修改。生命周期包括待确认、监控中、已暂停、已触发、冷却中、部分数据异常、已到期和已归档。休市的价格检查与用户手动暂停分别处理，公告条件仍按适用规则检查。

价格条件按交易日与规则版本去重。公告以股票代码、标题和发布日期形成事件指纹；来源未稳定提供唯一公告号。同一事件不重复提醒。

冷却期间继续检查，新公告先保留。冷却结束后重新判断组合条件，满足规则才写入提醒，同时提交已处理指纹。修改规则保留版本记录，已有冷却仍然生效。

任务状态、检查记录、提醒和去重信息通过SQLite事务一起保存。重启后读取已保存状态继续运行。停机期间的价格路径无法用当前快照补回；公告在接口支持的窗口内重叠回查，最大补查30天，不承诺完整补齐所有事件。

## 数据来源与异常处理

| 来源或异常 | 实现方式 | 用户受到的影响 |
| --- | --- | --- |
| 扶摇行情与交易日历 | 后台直接请求官方REST API，验证代码、价格、昨收及时间 | 有效行情才参与判断；休市不会把旧价格当作新行情 |
| iFinD公告 | 后台通过已配置的Node调用模块执行MCP公告检索 | 首次成功查询建立已有公告基线，此后识别新发现事件 |
| 行情超时或服务错误 | 有限重试；限流时等待后续检查 | 价格条件暂时无法判断，其他有效条件仍可检查 |
| 数据过期、缺失或冲突 | 拒绝用异常价格判断 | 显示受影响的条件及原因 |
| 公告服务异常 | 保留检查游标，后续重试与补查 | 公告条件无法判断，价格条件独立运行 |
| 模拟体验 | 使用可控行情与故障情景，和真实任务隔离 | 可复现触发、未触发、冷却和恢复过程 |

公告采用有限的语义检索，单次最多20条。空结果表示本次没有检索到匹配项，不代表市场上没有公告。返回日期不等于精确发布时间。真实任务不会接受模拟事件注入。

每条条件有“满足”“未满足”“无法判断”三种结果。OR中任一有效条件满足即可满足组合；AND必须全部满足。是否发提醒还需检查去重与冷却。检查记录保留规则版本、来源、时间、各条件值和最终原因。

## 验收与评测

| 验收对象 | 检查方法与证据 |
| --- | --- |
| 自然语言理解 | 固定20条输入逐项比较预期字段，模型输出和本地回退分别计数 |
| 主流程与异常处理 | 自动化检查创建、修改、边界比较、来源异常与恢复 |
| 去重与冷却 | 重复价格和公告不会重复打扰，新公告可在冷却结束后补发 |
| 中断恢复 | 独立服务进程终止后重启，核对已保存状态和待提醒事件 |
| 可解释性 | 连续检查与对应记录逐项核对；旧提醒保留生成时依据 |
| 用户使用效果 | 运行页分开显示模拟与真实样本；真人留存和实际漏报率尚未测量 |

已记录122项Python测试、16项前端测试通过。固定20条模型用例的联网复测均取得模型输出且字段匹配；该结果不代表任意输入准确率。方法、运行批次与原始证据见[测试说明](TESTING_AND_EVAL.md)和[AI使用记录](AI_USAGE_AND_VERIFICATION.md)。

## 当前取舍与后续顺序

本次选择单机SQLite和异步调度，减少部署依赖，并用事务保持提醒与状态一致。更大规模需要独立工作队列与任务租约，当前未实现多节点调度。

公开入口依赖本机和临时通道；常驻服务器及固定地址尚未部署。访客通过浏览器Cookie隔离，清除Cookie或换设备无法自动找回任务。

后续优先完善公告覆盖和事件身份，再补充外部通知与常驻部署。全市场搜索、跨设备账户和复杂时序条件需要进一步扩展。具体决策见[产品决策](docs/PRODUCT_DECISIONS.md)，部署条件见[部署说明](docs/DEPLOYMENT.md)。
